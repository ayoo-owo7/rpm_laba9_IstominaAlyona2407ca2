<Window x:Class="Rpm_Lab8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rpm_Lab8"
        mc:Ignorable="d"
        Title="Телефонная книга (MVVM)" Height="420" Width="520"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Имя:" VerticalAlignment="Center" Margin="0,0,5,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Телефон:" VerticalAlignment="Center" Margin="0,0,5,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <Button Grid.Row="2" Grid.Column="1" Content="➕ Добавить" Command="{Binding AddCommand}" HorizontalAlignment="Left" Margin="0,5" Padding="10,3"/>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Foreground="Crimson" FontSize="13" Text="{Binding ErrorMessage}" Margin="0,8,0,5"/>

        <DataGrid Grid.Row="4" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding Contacts}" 
                  SelectedItem="{Binding SelectedContact}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  Margin="0,5,0,10" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="5" Grid.ColumnSpan="2" Content="🗑️ Удалить выбранный" 
                Command="{Binding DeleteCommand}" 
                CommandParameter="{Binding SelectedContact}" 
                HorizontalAlignment="Center" Padding="15,5"/>
    </Grid>
</Window>
